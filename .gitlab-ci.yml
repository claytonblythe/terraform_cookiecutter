image: hashicorp/terraform:latest


before_script:
  - rm -rf .terraform
  - terraform --version
  - echo $(pwd)
  - terraform init

stages:
  - plan

plan:
  stage: plan
  script:
    - terraform init
